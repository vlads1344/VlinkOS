🛠 Системное ядро (Internal)
Автономный запуск: Через start.bat система всегда запускается из правильной директории, исключая ошибки путей.

Интерактивный Shell: Командная строка с поддержкой аргументов и разделением на системные/внешние команды.

Умная навигация:

cd <path> — переход по папкам.

rm — полная очистка терминала.

rst — горячая перезагрузка системы без закрытия окна.

Продвинутый ls: Теперь поддерживает просмотр любой папки (например, ls apps) и выводит:

Тип объекта: [D] — папка, [F] — файл.

Размер файлов в байтах.

Цветовую индикацию.

🔐 Безопасность и Root (Cloud-based)
Команда sudo: Позволяет выполнять защищенные действия.

Remote Auth: Пароль не хранится на ПК. Система делает запрос к твоему серверу (check_root.php), что делает локальный взлом бесполезным.

Сессионный доступ: Привилегии выдаются только на выполнение конкретной команды.

📦 Менеджер пакетов и сеть
vpm (через apt install): Полноценная установка софта с твоего сервера Ubuntu.

Автоматическое скачивание .py файлов в папку /apps.

Проверка наличия пакета в удаленном репозитории.

vget: Универсальный загрузчик. Можно скачать любой файл (картинку, скрипт, конфиг) по прямой ссылке в систему.

ping: Проверка доступности твоего сервера или любого сайта.

📂 Работа с файлами (File Management)
create <name>: Универсальный инструмент. Создает файл, если есть расширение (например, .txt), или папку, если расширения нет.

edit <file>: Встроенный текстовый редактор. Можно открывать существующие файлы, редактировать их и сохранять через :q.

rm <target>: Умное удаление. Удаляет файлы сразу, а при попытке удалить папку — запрашивает подтверждение (защита от случайной очистки диска).

🎮 Развлечения (Entertainment)
neofetch: Красивое инфо-окно с ASCII-логотипом и данными о железе (исправлено: работает без предупреждений SyntaxWarning).

minesweeper: Классический Сапер прямо в консоли. Поддержка координат и генерация случайных минных полей.
